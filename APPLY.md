# ハブ画面の刷新（差し替え手順）

対象リポジトリ: `sankisuguya-create/arithmetic-timeattack`

## 置き換えるファイル

| 置き場所 | 種別 |
|---|---|
| `apps/hub/Code.gs` | 全文差し替え |
| `apps/hub/index.html` | 全文差し替え |
| `apps/hub/teacher.html` | 全文差し替え |

`common/` と `apps/kuku` `apps/length` は無変更。
`links` `roster` `config` の各シートも構造変更なし。

## Code.gs の変更点（2か所のみ）

1. `COLORS` の6色を彩度の高い値に置換。キー名は据え置きなので `links` シートの `color` 列は書き換え不要。
2. `setVisible(id, visible)` を追加。`saveLinks` の直後、`saveHubConfig` の手前。

## index.html の変更点

- 単元名とページ見出しに Zen Old Mincho 900 を当てる。Google Fonts の `text=` に
  実際に描く文字だけを渡すサブセット読み込み。取得失敗時は Chrome OS 同梱の
  Noto Serif CJK に落ちるので表示は壊れない。
- タイルの高さを固定（`min-height` → `height:118px`）。
- 単元名は最大2行。`2.35rem` から `1.1rem` まで6段階を上から試し、
  実際の描画高さがタイル内に収まる最大サイズを選ぶ。Webフォント到着後に再計測する。
- 折り返しは `word-break:auto-phrase`（Chrome 119+）。未対応環境では従来どおり任意位置で折る。

## teacher.html の変更点

- 「表示する」チェックボックスを廃止し、各行の見出しに「公開中／非公開」のピルを設置。
  1クリックで `setVisible` を呼び、該当行だけ書き換えてキャッシュを捨てる。
  保存ボタンを押す必要はない。通信失敗時は表示を元に戻す。
- 未保存の新規リンク（`id` が空）は切替できないので、その旨を出して保存を促す。

## デプロイ後の確認

1. 教師画面で「公開中／非公開」を押し、`links` シートの G 列が即座に変わること。
2. 児童アカウントでハブを開き、単元名が2行に折れてもタイルの高さが揃うこと。
3. 単元名が6文字（例「時こくと時間」）のとき、折り位置と説明文の詰まり方。
4. 校内のURLフィルタが `fonts.googleapis.com` を通すか。

## 既知の制約

`links` のキャッシュTTLが60秒あるため、公開／非公開の切替はすでにハブを開いている
児童には最大1分遅れて反映される。TTLを0にすると解消するが、30台同時アクセスのたびに
シートを読むので `boot()` が遅くなる。現状はTTLを維持している。

## コミットメッセージ案

```
hub: 明朝ベースの画面刷新と公開切替のワンクリック化

- 単元名とタイトルを Zen Old Mincho に変更（text= サブセットで数KB）
- タイルの高さを固定し、長い単元名は2行に折って自動縮小
- COLORS を彩度の高い6色に更新（キー名は据え置き）
- setVisible を追加し、教師画面の公開切替を1クリックに
```
